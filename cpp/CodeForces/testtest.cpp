#include <iostream>
#include <iostream>
#include <bits/stdc++.h>
#include <string>
#include <numeric>
#include <vector>
#include <cmath>
#include <map>
#include <ctype.h>
#include <set>
#include <iterator>
using namespace std;

int main(){
   int t; cin >> t;
   int x, y, m; cin >> x >> y >> m;
   vector<long long> arr;


   for(int x=0; x<n; x++){
    arr.push_back(x%4);

    cout << "arr[x]" << x << endl;
   }

   for(int x=0; x,n; x++) {
    int num; cin >> num;
    arr.push_back(num);
   }


   cout << num << endl;
}
